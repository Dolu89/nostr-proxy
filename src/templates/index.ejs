<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="lightning" content="lnurlp:dolu89@ln.tips"/>
  <meta property="og:image" content="https://pbs.twimg.com/profile_images/1577320325158682626/igGerO9A_400x400.jpg" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://api.fonts.coollabs.io" crossorigin>
  <link href="https://api.fonts.coollabs.io/css2?family=Inter:wght@400;500;900&display=swap" rel="stylesheet">
  <title>Nostr Proxy</title>
  <style>
    html,
    body {
      color: #666;
      margin: 0;
      padding: 0;
      font-family: 'Inter', sans-serif;
    }

    a {
      color: #CE9FFC;
      text-decoration: none;
    }

    a:hover {
      color: #7367F0;
    }

    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: space-between;
      width: 100%;
      height: 100%;
    }

    .header {
      display: flex;
      flex-direction: column;
      align-items: end;
      justify-content: center;
      margin-bottom: 50px;
    }

    .header>.title {
      font-size: 4.5rem;
      font-weight: 900;
      margin: 0;
    }

    .header>.title>.gradient {
      background: -webkit-linear-gradient(45deg, #CE9FFC, #7367F0);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .content {
      margin-top: 60px;
    }

    .copy-text {
      width: 100%;
      padding: 10px;
      border-radius: 5px;
      font-size: 1.2rem;
      font-weight: 500;
      color: #000;
      background: linear-gradient(white, white) padding-box,
        linear-gradient(to right, #CE9FFC, #7367F0) border-box;
      border-radius: 4px;
      border: 3px solid transparent;
      text-align: center;
      margin-bottom: 30px;
    }

    .footer {
      padding: 10px 30px;
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      display: flex;
      gap: 30px;
      align-items: baseline;
      justify-content: end;
      background: #000;
      color: #fff;
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="content">

      <div class="header">
        <h1 class="title">Nostr <span class="gradient">Proxy</span></h1>
        <span class="subtitle">Using this proxy, you save <%- total %> WS connections.</span>
      </div>

      <input type="text" disabled class="copy-text" value="<%- proxyUrl %>">

      <span>🟢 Connected <%- connected.length %>/<%- total %></span>
      <ul>
        <% connected.forEach(function(url){ %>
        <li><%- url %></li>
        <% }); %>
      </ul>

      <span>🔴 Diconnected</span>
      <ul>
        <% disconnected.forEach(function(url){ %>
        <li><%- url %></li>
        <% }); %>
      </ul>
    </div>

  </div>
  <div class="footer">
    <span> <a href="https://github.com/dolu89/nostr-proxy">Github</a></span>
    <span>Made with 💩 by Dolu</span>
    <span>⚡️dolu89@ln.tips</span>
  </div>

</body>

</html>